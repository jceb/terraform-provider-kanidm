---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kanidm_system_denied_names Resource - kanidm"
subcategory: "System"
description: |-
  Manages the system-wide list of denied user names.
  Users in Kanidm can change their name at any time. This resource maintains a list of names that are not allowed to be used, preventing conflicts with system accounts or excluding abusive terms.
  This is a singleton resource — only one kanidm_system_denied_names resource should exist in your configuration.
  Example Usage
  
  resource "kanidm_system_denied_names" "this" {
    names = [
      "administrator",
      "superuser",
      "system",
      "guest",
    ]
  }
---

# kanidm_system_denied_names (Resource)

Manages the system-wide list of denied user names.

Users in Kanidm can change their name at any time. This resource maintains a list of names that are not allowed to be used, preventing conflicts with system accounts or excluding abusive terms.

This is a singleton resource — only one `kanidm_system_denied_names` resource should exist in your configuration.

## Example Usage

```hcl
resource "kanidm_system_denied_names" "this" {
  names = [
    "administrator",
    "superuser",
    "system",
    "guest",
  ]
}
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `names` (Set of String) Set of denied names. Names in this list cannot be used as Kanidm account names.

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

```shell
terraform import kanidm_system_denied_names.this denied_names
```

This resource depends on the Kanidm system configuration entry being present and readable on the target instance.
