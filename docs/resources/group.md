---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kanidm_group Resource - kanidm"
subcategory: "Groups"
description: |-
  Manages a Kanidm group.
  Groups are used to organize users and service accounts, and control access to resources.
  Example Usage
  
  resource "kanidm_group" "developers" {
    name        = "developers"
    description = "Development team members"
  
    members = [
      kanidm_person.alice.id,
      kanidm_person.bob.id,
      kanidm_service_account.ci.id,
    ]
  }
---

# kanidm_group (Resource)

Manages a Kanidm group.

Groups are used to organize users and service accounts, and control access to resources.

## Example Usage

```hcl
resource "kanidm_group" "developers" {
  name        = "developers"
  description = "Development team members"

  members = [
    kanidm_person.alice.id,
    kanidm_person.bob.id,
    kanidm_service_account.ci.id,
  ]
}
```

## Example Usage

```terraform
# Example: Group with person and service account members
resource "kanidm_group" "developers" {
  name        = "developers"
  description = "Development team members"

  members = [
    kanidm_person.alice.id,
    kanidm_person.bob.id,
    kanidm_service_account.ci.id,
  ]
}

# Example: Group with only person members
resource "kanidm_group" "admins" {
  name        = "infrastructure-admins"
  description = "Infrastructure administrators with full access"

  members = [
    kanidm_person.alice_password.id,
    kanidm_person.charlie.id,
  ]
}

# Example: Empty group (members added later)
resource "kanidm_group" "monitoring" {
  name        = "monitoring-users"
  description = "Users with monitoring access"
}

# Example: Group with dynamic membership
resource "kanidm_group" "all_staff" {
  name        = "all-staff"
  description = "All staff members"

  members = [
    for person in kanidm_person.staff : person.id
  ]
}

# Example: Imported existing group
# Import command: terraform import kanidm_group.existing_group group_name
resource "kanidm_group" "existing_group" {
  name        = "existing"
  description = "Existing Group"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Group name. This can be changed outside Terraform, but is tracked via the stable UUID in `id`.

### Optional

- `description` (String) Description of the group.
- `entry_managed_by` (Set of String) Set of account or group IDs that can manage this group.
- `gidnumber` (Number) Optional POSIX gidnumber for the group. Computed after POSIX is enabled, even when Kanidm generates the value.
- `mail` (List of String) Email addresses for the group.
- `members` (Set of String) Set of member IDs (persons, service accounts, or groups). Members are managed as a complete set - any changes will replace all members.
- `posix_enabled` (Boolean) Whether POSIX support is enabled for the group. Enabling without a gidnumber lets Kanidm generate one automatically. Disabling after enablement is not currently supported.

### Read-Only

- `id` (String) Stable Kanidm UUID for this group. This value is computed after creation/import and used to keep the resource linked across external renames.
